{
  "name": "super-coach-pro",
  "private": true,
  "scripts": {
    "dev": "npm run dev     --prefix UI",
    "build": "npm run build   --prefix UI",
    "preview": "cd server && npm run dev",
    "lint": "npm run lint    --prefix UI",
    "deploy": "cd server && npm run deploy"
  },
  "devDependencies": {
    "wrangler": "^4.76.0"
  }
}
